# معماری پروژه Krelz Network

## نمای کلی

Krelz Network یک شبکه غیرمتمرکز LLM است که از سه لایه اصلی تشکیل شده است:

1. **لایه کاربر (Frontend)**: رابط کاربری برای ماینرها و کاربران
2. **لایه واسط (Backend)**: API سرور و هماهنگی
3. **لایه بلاکچین (Blockchain)**: قراردادهای هوشمند و پرداخت

---

## نمودار معماری

```
┌─────────────────────────────────────────────────────────────┐
│                    لایه کاربر (Frontend)                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  وب‌سایت   │  │ اپ ماینر   │  │ کیف پول    │         │
│  │  (Next.js)  │  │ (Electron)  │  │ (MetaMask)  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    لایه واسط (Backend)                       │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  API Server │  │  Database   │  │    Ollama   │         │
│  │  (Node.js)  │  │ (PostgreSQL)│  │  (LLM)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  لایه بلاکچین (BSC)                          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ KrelzToken  │  │ StakingPool │  │ BurnMechanism│         │
│  │  (ERC-20)   │  │             │  │             │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

---

## جریان داده

### ۱. جریان درخواست کاربر

```
کاربر → فرانت‌اند → بک‌اند → Ollama → پردازش → پاسخ
                │
                ▼
          کسر توکن + سوزاندن
```

### ۲. جریان ماینینگ

```
ماینر → اپلیکیشن → اتصال به شبکه → دریافت درخواست → پردازش → گزارش
                                                    │
                                                    ▼
                                              دریافت پاداش
```

### ۳. جریان پرداخت

```
کاربر → پرداخت توکن → قرارداد هوشمند → تقسیم پاداش
                                    │
                                    ├── ۱۰٪ ماینر
                                    ├── ۱۰٪ پلتفرم
                                    └── ۱٪ سوزاندن
```

---

## اجزای فنی

### فرانت‌اند (Next.js)
- رابط کاربری چت LLM
- پنل ماینر و کاربر
- اتصال کیف پول MetaMask
- نمایش آمار و اطلاعات

### بک‌اند (Node.js/Express)
- API احراز هویت
- API مدیریت ماینرها
- API مدیریت کاربران
- API پردازش LLM
- API پرداخت

### دیتابیس (PostgreSQL)
- جدول کاربران
- جدول ماینرها
- جدول درخواست‌ها
- جدول تراکنش‌ها

### Ollama
- اجرای مدل LLM
- مدیریت GPU
- بهینه‌سازی عملکرد

### قرارداد هوشمند (BSC)
- KrelzToken (ERC-20)
- StakingPool (استیکینگ)
- BurnMechanism (سوزاندن)

---

## امنیت

### احراز هویت
- JWT Token
- MetaMask Signature
- Rate Limiting

### داده‌ها
- رمزنگاری حساسیت
- Backup خودکار
- SSL/TLS

### بلاکچین
- Multi-signature (آینده)
- Audit قراردادها
- Timelock

---

## مقیاس‌پذیری

### افقی
- Load Balancer
- Multiple API Servers
- Database Replication

### عمودی
- CPU/RAM Upgrade
- GPU Addition
- Storage Upgrade

---

## نظارت

### لاگ‌ها
- Application Logs
- Access Logs
- Error Logs

### مانیتورینگ
- CPU/RAM Usage
- Network Traffic
- API Response Time

### هشدارها
- Email Alerts
- Telegram Bot
- SMS (آینده)
