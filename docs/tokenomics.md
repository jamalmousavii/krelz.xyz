# توکنومیکس KRELZ

## خلاصه

| مورد | مقدار |
|------|-------|
| نام توکن | Krelz |
| نماد | KRELZ |
| بلاکچین | BNB Chain (BSC) |
| استاندارد | ERC-20 (BEP-20) |
| تعداد کل | 1,000,000,000 |
| دیزیمال | 18 |

---

## توزیع توکن

```
تعداد کل: 1,000,000,000 (1 میلیارد) توکن

├── 60% ماینینگ (600,000,000)
│   ├── GPU ماینینگ: 400,000,000
│   ├── CPU ماینینگ: 100,000,000
│   └── Uptime پاداش: 100,000,000
│
├── 20% اکوسیستم (200,000,000)
│   ├── استخر نقدینگی: 100,000,000
│   ├── گرنت‌ها: 50,000,000
│   └── بازاریابی: 50,000,000
│
├── 10% تیم (100,000,000)
│   └── Vested 24 ماهه
│
└── 10% فاندریشن (100,000,000)
    └── توسعه آینده
```

---

## مکانیزم سوزاندن (Burning)

### ۱. کارمزد تراکنش
```
۱٪ از هر تراکنش سوزانده می‌شود
مثال: ۱۰۰ توکن پرداخت → ۱ توکن سوزانده
```

### ۲. کارمزد استفاده از LLM
```
۲٪ از هر درخواست LLM سوزانده می‌شود
مثال: ۵۰ توکن برای چت → ۱ توکن سوزانده
```

### ۳. جریمه ماینر
```
۵٪ از استیک ماینر متخلف سوزانده می‌شود
```

### ۴. سوزاندن دوره‌ای
```
هر ۳ ماه: ۱٪ از موجودی فاندریشن سوزانده می‌شود
```

---

## پاداش ماینرها

### فرمول پاداش
```
Reward = (GPU_Score × Uptime_Factor × Task_Score) / Total_Network_Power
```

### GPU Score (40% وزن)
| GPU | ضریب |
|-----|------|
| RTX 3060 | 1.0x |
| RTX 3070 | 1.5x |
| RTX 3080 | 2.0x |
| RTX 3090 | 2.5x |
| RTX 4060 | 1.2x |
| RTX 4070 | 1.8x |
| RTX 4080 | 2.3x |
| RTX 4090 | 3.0x |

### Uptime Factor (30% وزن)
| Uptime | ضریب |
|--------|------|
| 99-100% | 1.0x |
| 95-99% | 0.9x |
| 90-95% | 0.8x |
| 80-90% | 0.6x |
| <80% | 0.3x |

### Task Score (30% وزن)
| فعالیت | پاداش |
|--------|-------|
| پاسخ به کاربر | +0.1 |
| رضایت کاربر (هر ستاره) | +0.2 |
| زمان پاسخگویی (سریع‌تر) | +0.1 |
| دقت پاسخ | +0.2 |

---

## درآمد پلتفرم

### تقسیم درآمد
```
درآمد کل: ۲۰٪

├── ۱۰٪ از ماینرها
│   └── از کل پاداش ماینرها
│
└── ۱۰٪ از کاربران
    └── از کل هزینه استفاده
```

### مثال
```
ماینر: GPU با قیمت 1000 توکن
├── کاربر: 2000 توکن پرداخت می‌کند
├── ماینر: 1700 توکن دریافت می‌کند
├── پلتفرم: 200 توکن (10% ماینر + 10% کاربر)
└── سوزاندن: 20 توکن (1% تراکنش + 2% LLM)
```

---

## نقشه راه توکن

| مرحله | زمان | عملیات |
|-------|------|--------|
| TGE | ماه ۰ | عرضه اولیه ۱۰٪ |
| Phase 1 | ماه ۱-۳ | استیکینگ فعال |
| Phase 2 | ماه ۳-۶ | لیست صرافی |
| Phase 3 | ماه ۶-۱۲ | گسترش اکوسیستم |
| Phase 4 | سال ۲+ | DAO و حاکمیت |

---

## قرارداد هوشمند

### KrelzToken.sol
```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract KrelzToken is ERC20, Ownable {
    uint256 public constant MAX_SUPPLY = 1_000_000_000 * 10**18;
    
    constructor() ERC20("Krelz", "KRELZ") Ownable(msg.sender) {
        _mint(msg.sender, MAX_SUPPLY);
    }
    
    function burn(uint256 amount) public {
        _burn(msg.sender, amount);
    }
    
    function burnFrom(address account, uint256 amount) public {
        _burn(account, amount);
    }
}
```

---

## آدرس قرارداد

```
BSC Mainnet: 0x0000000000000000000000000000000000000000
BSC Testnet: 0x0000000000000000000000000000000000000000
```

> ⚠️ آدرس‌ها پس از Deploy به‌روزرسانی می‌شوند.
